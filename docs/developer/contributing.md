# Contributing

The working agreement moved to the repository root so it sits where
contributors and forges expect it: [`CONTRIBUTING.md`](../../CONTRIBUTING.md).

Everything that was here — the enforced invariants, how to add an operation or
an error code, how to run the tests and gates — lives there now, in one place,
so the two copies cannot drift apart.
